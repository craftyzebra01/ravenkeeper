- Add a grimoire screen between preGame and firstNight.
    - the transition is abrasive. this will make it smoother.
    - used for game master examinations
    - supports gm adding night time messages to the players if desired.

- I don't think assign roles is actually using the selected roles.
- Players display and role selection should take up the same space.
    This way you can go players > roles > start instead of
    players > roles > players > start.
